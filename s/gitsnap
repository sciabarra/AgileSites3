msg=${1:?commit}
subs="lib plugin setup bigbang"
for i in $subs
do cd $i 
   git commit -m "$msg" -a 
   git push origin 11g
   cd ..
done
git add lib plugin setup bigbang
git commit -m "$msg" -a
git push origin 11g 
