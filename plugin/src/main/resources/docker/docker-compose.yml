shared:
  build: sites-shared
  hostname: shared
  domainname: loc
  container_name: shared
  ports:
    - 1521:1521
    - 8080:8080
  volumes:
    - "/app/shared"

admin:
  build: sites-admin
  hostname: admin
  domainname: loc
  container_name: admin
  links:
    - shared:shared.loc
  ports:
    - 7001:7001

